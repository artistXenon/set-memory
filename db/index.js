const {
    createCase, readCases, deleteCase,
    createSet, readSets, deleteSet,
    readTest, updateTest, flushTest
} = require('./connection')

module.exports = {
    createCase, readCases, deleteCase,
    createSet, readSets, deleteSet,
    readTest, updateTest, flushTest
}